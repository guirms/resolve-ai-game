export interface CountyInfo {
    name: string;
    value: number;
    isSelected: boolean;
}

export interface CurrentNumber {
    number: number;
    hint: string; 
    dayContentIndex: number;
    hintIndex: number;
}